let bodyParser,express,regularRouter,path;_e4b‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_e4b‍.w("express",[["default",["express"],function(v){express=v}]]);_e4b‍.w("./routes",[["default",["regularRouter"],function(v){regularRouter=v}]]);_e4b‍.w("path",[["default",["path"],function(v){path=v}]]);// imports






const methodOverride = require('method-override')

const routersubcompanies = require('./api/routes/subCompaniesRoutes');
const locationRouter=require('./api/routes/locationRoutes');

const app = express();

const PORT = process.env.PORT || 8000;



// parse application/x-www-form-urlencoded
app.use(bodyParser.urlencoded({ extended: false }));

// app.use(methodOverride('_method'))

// app.use(express.json())

// // parse application/json
// app.use(bodyParser.json());

//static files


app.use('/', express.static(path.join(__dirname, 'public')));
app.use('/SubCompanies', express.static(path.join(__dirname, 'public')));
app.use('/Locations', express.static(path.join(__dirname, 'public')));


// set views
app.set('views', './views');
app.set('view engine', 'ejs');


// routes

app.use('/', regularRouter);
app.use('/SubCompanies',routersubcompanies);
app.use('/Locations',locationRouter);

app.listen(PORT, () => {
   console.log(`Server Started at Port ${PORT}`)
}

);
